import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jack.dice_revolver.app',
  appName: 'dice-revolver',
  webDir: 'dist'
};

export default config;
